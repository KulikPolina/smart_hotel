import 'package:flutter/material.dart';

class LightSwitcher extends StatelessWidget {
  const LightSwitcher({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}